Read and execute the full command protocol from commands/assign-reviewers.md.
This command is a standalone utility — it does NOT require project-state.md or pipeline prerequisites.
Do NOT modify project-state.md when running this command.
