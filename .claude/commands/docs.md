Read and execute the full command protocol from commands/docs.md.
This is a utility command — it can run anytime, outside the sequential pipeline.
Load the app's CODEBASE-CONTEXT.md if it exists.
Activate the Documentation Agent from agents/docs-agent.md.
